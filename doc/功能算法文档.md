# 容量表合成工具 - 功能算法文档

## 1. 概述

本工具用于合并电信运营商的 4G/5G 小区容量报表数据，自动生成综合容量分析表。主要功能包括：

- 自动识别并加载最新的源数据文件
- 按网络制式（4G/5G）分别合成容量表
- 生成 45G 联合总表
- 智能计算流量系数、负荷情况、长尾小区分类等衍生指标
- 通过共站同覆盖关系补充扇区等字段

---

## 2. 输入文件

### 2.1 文件匹配模式

| 数据类型 | 文件名模式 | 说明 |
|---------|-----------|------|
| 5G 周容量 | `5G小区容量-周*.xlsx` | 5G 小区周粒度容量数据 |
| 5G 天容量 | `5G小区容量报表*.xlsx` | 5G 小区天粒度容量数据 |
| 5G MR 覆盖 | `5GMR覆盖-小区天*.xlsx` | 5G MR 测量报告覆盖数据 |
| 5G KPI | `5G小区性能KPI报表*.xlsx` | 5G 小区性能 KPI 数据 |
| 4G 周容量 | `重要场景-周*.xlsx` | 4G 重要场景周数据 |
| 4G 天容量 | `重要场景-天*.xlsx` | 4G 重要场景天数据 |
| 4G MR 覆盖 | `4GMR覆盖-小区天*.xlsx` | 4G MR 测量报告覆盖数据 |
| 共站同覆盖 | `共站同覆盖小区_4g_5g.xlsx` | 4G/5G 共站同覆盖映射表 |

### 2.2 输入表格字段详情

#### 2.2.1 5G 周容量表 (5G小区容量-周*.xlsx)

| 字段名 | 类型 | 用途 |
|-------|------|------|
| NCGI | 字符串 | 小区唯一标识 |
| 记录开始时间 | 日期时间 | 统计周期开始 |
| 记录结束时间 | 日期时间 | 统计周期结束 |
| 地市 | 字符串 | 所属地市 |
| 网元状态 | 字符串 | 小区运行状态 |
| 小区名称 | 字符串 | 小区名称 |
| 场景1 / 一级场景 | 字符串 | 场景分类 |
| 使用频段 | 字符串 | 频段信息 |
| 站点名称 | 字符串 | 所属物理站 |
| 是否高负荷待扩容小区 / 是否高负荷 | 字符串 | 扩容标识 |

#### 2.2.2 5G 天容量表 (5G小区容量报表*.xlsx)

| 字段名 | 类型 | 用途 |
|-------|------|------|
| NCGI | 字符串 | 小区唯一标识 |
| 记录开始时间 | 日期时间 | 日期 |
| 日RLC层上下行总流量(G) | 数值 | 日总流量 |
| 忙时小区PRB利用率(%) | 数值 | 忙时PRB利用率 |
| 忙时上行PRB平均利用率(%) | 数值 | 上行PRB |
| 忙时下行PRB平均利用率(%) | 数值 | 下行PRB |
| 忙时PDCCH信道CCE占用率(%) | 数值 | PDCCH占用率 |
| RRC连接最大数-忙时 | 数值 | 忙时最大RRC |
| RRC连接平均数-忙时 | 数值 | 忙时平均RRC |
| 忙时RLC层上行业务字节数(G) | 数值 | 忙时上行流量 |
| 忙时RLC层下行业务字节数(G) | 数值 | 忙时下行流量 |

#### 2.2.3 5G MR 覆盖表 (5GMR覆盖-小区天*.xlsx)

| 字段名 | 类型 | 用途 |
|-------|------|------|
| 小区NCGI | 字符串 | 小区唯一标识 |
| 移动RSRP采样的总采样点 | 数值 | MR总采样点 |
| 移动RSRP采样强于-110采样点 | 数值 | 强覆盖采样点 |
| 移动平均TA(M) | 数值 | 平均TA（米） |

#### 2.2.4 5G KPI 表 (5G小区性能KPI报表*.xlsx)

| 字段名 | 类型 | 用途 |
|-------|------|------|
| NCGI | 字符串 | 小区唯一标识 |
| VoNR语音话务量 | 数值 | VoNR语音话务量 |

#### 2.2.5 4G 周容量表 (重要场景-周*.xlsx)

| 字段名 | 类型 | 用途 |
|-------|------|------|
| CGI | 字符串 | 小区唯一标识 |
| 记录开始时间 | 日期时间 | 统计周期开始 |
| 记录结束时间 | 日期时间 | 统计周期结束 |
| 所属地市 | 字符串 | 所属地市 |
| 网元状态 | 字符串 | 小区运行状态 |
| 小区名称 | 字符串 | 小区名称 |
| 场景 | 字符串 | 场景分类 |
| 使用频段 / 频点 | 字符串 | 频段信息 |
| 所属站点名称 | 字符串 | 所属物理站 |
| 是否高负荷待扩容小区 | 字符串 | 扩容标识 |
| 是否高流量预警小区 | 字符串 | 预警标识 |
| 自忙时上行PRB平均利用率 | 数值 | 周表PRB指标 |
| 自忙时下行PRB平均利用率 | 数值 | 周表PRB指标 |
| 自忙时PDCCH信道CCE占用率 | 数值 | 周表CCE指标 |

#### 2.2.6 4G 天容量表 (重要场景-天*.xlsx)

| 字段名 | 类型 | 用途 |
|-------|------|------|
| CGI | 字符串 | 小区唯一标识 |
| 记录开始时间 | 日期时间 | 日期 |
| 日4G流量（GB） | 数值 | 日总流量 |
| 自忙时上行PRB平均利用率 | 数值 | 上行PRB |
| 自忙时下行PRB平均利用率 | 数值 | 下行PRB |
| 自忙时PDCCH信道CCE占用率 | 数值 | PDCCH占用率 |
| 自忙时有效RRC连接最大数 | 数值 | 有效RRC最大 |
| 自忙时RRC连接最大数 | 数值 | RRC最大 |
| 自忙时有效RRC连接平均数 | 数值 | RRC平均 |
| 自忙时空口上行业务字节数 | 数值 | 忙时上行流量 |
| 自忙时空口下行业务字节数 | 数值 | 忙时下行流量 |
| VOLTE语音话务量 | 数值 | VOLTE语音话务 |

#### 2.2.7 4G MR 覆盖表 (4GMR覆盖-小区天*.xlsx)

| 字段名 | 类型 | 用途 |
|-------|------|------|
| cgi | 字符串 | 小区唯一标识 |
| MRO移动总采样点 | 数值 | MR总采样点 |
| MRO移动大于等于负110DBM的采样点数 | 数值 | 强覆盖采样点 |
| 平均TA | 数值 | 平均TA |

#### 2.2.8 共站同覆盖映射表 (共站同覆盖小区_4g_5g.xlsx)

| 字段名 | 类型 | 用途 |
|-------|------|------|
| CGI | 字符串 | 小区唯一标识（主键） |
| 共站同覆盖名 | 字符串 | 扇区名称 |
| 路测网格 | 字符串 | 路测网格信息 |
| 乡镇街道 | 字符串 | 所属乡镇街道 |
| 是否覆盖层 | 字符串 | 是否覆盖层标识 |
| 小区所属区域 | 字符串 | 所属区域 |

### 2.3 文件选择策略

```python
pick_latest_file(pattern: str) -> Path
```

对于每个数据类型的文件，按以下规则选择：
1. 使用 `glob` 匹配所有符合模式的文件
2. 从文件名中提取日期（正则: `\d{4}-\d{2}-\d{2}`）
3. 按日期降序 + 文件名升序排序
4. 返回最新版本的文件

---

## 3. 核心数据处理算法

### 3.1 时间字段处理

**判断是否周末：**

```python
day["记录开始时间"] = normalize_datetime(day["记录开始时间"])
day["是否周末"] = day["记录开始时间"].dt.weekday >= 5
```

- `weekday >= 5` 表示周六(5)或周日(6)

**时间戳提取：**

从周表的"记录开始时间"和"记录结束时间"字段提取时间范围，格式化为 `YYYYMMDD_YYYYMMDD` 用于输出文件名。

---

### 3.2 分组聚合算法

#### 3.2.1 5G 天数据聚合

```python
day_group = day.groupby("NCGI", dropna=False).agg(
    自忙时利用率=(util_col_5g, "mean"),
    日均流量=("日RLC层上下行总流量(G)", "mean"),
    自忙时上行PRB平均利用率=("忙时上行PRB平均利用率(%)", "mean"),
    自忙时下行PRB平均利用率=("忙时下行PRB平均利用率(%)", "mean"),
    自忙时PDCCH信道CCE占用率=("忙时PDCCH信道CCE占用率(%)", "mean"),
    自忙时RRC连接最大数=("RRC连接最大数-忙时", "mean"),
    自忙时有效RRC连接平均数=("RRC连接平均数-忙时", "mean"),
    自忙时上行流量=("忙时RLC层上行业务字节数(G)", "mean"),
    自忙时下行流量=("忙时RLC层下行业务字节数(G)", "mean"),
)
```

#### 3.2.2 工作日/周末分离聚合

```python
weekday_group = day.loc[~day["是否周末"]].groupby("NCGI", dropna=False).agg(...)
weekend_group = day.loc[day["是否周末"]].groupby("NCGI", dropna=False).agg(...)
```

- 工作日聚合：筛选 `weekday < 5` 的记录
- 周末聚合：筛选 `weekday >= 5` 的记录
- 均使用 `dropna=False` 保留 NCGI 为空的记录

#### 3.2.3 MR 覆盖数据聚合

```python
mr_group = mr.groupby("小区NCGI", dropna=False).agg(
    MRO移动总采样点=("移动RSRP采样的总采样点", "mean"),
    MRO强于110采样点合计=("移动RSRP采样强于-110采样点", "sum"),  # 累加求和
    MRO总采样点合计=("移动RSRP采样的总采样点", "sum"),           # 累加求和
    平均TA米=("移动平均TA(M)", "mean"),
).reset_index()

# 计算覆盖率
mr_group["MRO移动覆盖率"] = mr_group["MRO强于110采样点合计"] / mr_group["MRO总采样点合计"]
```

---

### 3.3 流量系数计算

```python
# 计算全网日均流量
avg_traffic = result["日均流量"].mean(skipna=True)

# 流量系数 = 小区日均流量 / 全网日均流量
result["流量系数"] = result["日均流量"] / avg_traffic

# 流量排名（升序，流量越小排名越前）
result["流量排名升序"] = result["日均流量"].rank(method="min", ascending=True)
```

**向量化的安全除法：**

```python
def safe_divide(numerator: pd.Series, denominator: pd.Series) -> pd.Series:
    denominator = denominator.replace(0, pd.NA)  # 零值替换为 NA
    return numerator.div(denominator)
```

---

### 3.4 长尾小区分类算法

**长尾阈值：** 取日均流量的 30% 分位数

```python
tail_threshold = result["日均流量"].quantile(0.3)
```

**分类逻辑（向量化实现）：**

```python
is_na_traffic = result["日均流量"].isna()
is_tail = (result["日均流量"] <= tail_threshold) & ~is_na_traffic
is_zero = result["日均流量"] == 0
is_high_util = result["自忙时利用率"].notna() & (result["自忙时利用率"] > 20)

result["长尾小区"] = pd.NA
result.loc[is_tail & is_zero, "长尾小区"] = "长尾具体原因待确认"      # 低流量且为零
result.loc[is_tail & ~is_zero & is_high_util, "长尾小区"] = "长尾待观察" # 低流量但高负荷
result.loc[is_tail & ~is_zero & ~is_high_util, "长尾小区"] = "长尾需处理" # 低流量且低负荷
```

**分类条件矩阵：**

| 日均流量状态 | 自忙时利用率 > 20% | 分类结果 |
|-------------|-------------------|---------|
| = 0 | 任意 | 长尾具体原因待确认 |
| ≤ 30%分位数 且 ≠ 0 | 是 | 长尾待观察 |
| ≤ 30%分位数 且 ≠ 0 | 否 | 长尾需处理 |

---

### 3.5 流量是否正常分类

```python
result["流量是否正常"] = pd.NA
result.loc[result["流量系数"] < 0.2, "流量是否正常"] = "低流量系数小区"
result.loc[(result["流量系数"] >= 0.2) & (result["流量系数"] < 3), "流量是否正常"] = "正常"
result.loc[result["流量系数"] >= 3, "流量是否正常"] = "高流量系数小区"
```

**分类标准：**

| 流量系数范围 | 分类 |
|------------|------|
| < 0.2 | 低流量系数小区 |
| 0.2 ~ 3 | 正常 |
| ≥ 3 | 高流量系数小区 |

---

### 3.6 负荷情况判定

#### 3.6.1 5G 负荷判定

```python
result["负荷情况"] = pd.NA
result.loc[result["自忙时利用率"].notna() & (result["自忙时利用率"] > 80), "负荷情况"] = "负荷高小区"
result.loc[result["自忙时利用率"].isna() | (result["自忙时利用率"] <= 80), "负荷情况"] = "正常"
```

#### 3.6.2 4G 负荷判定（基于小区名称类型）

```python
# 小区名称模式识别
is_rdc_dc = name.str.contains("RDC|DC-|RGS|GS-", regex=True, na=False)  # 数据中心/核心场景
is_rd = name.str.contains("RD-", regex=True, na=False)                  # 农村场景

# 不同场景不同阈值
result["负荷情况"] = "正常"
result.loc[is_rdc_dc & (util > 90), "负荷情况"] = "负荷高小区"           # 数据中心/核心：>90%
result.loc[is_rd & (util > 70) & ~is_rdc_dc, "负荷情况"] = "负荷高小区"  # 农村：>70%
result.loc[~is_rdc_dc & ~is_rd & (util > 50), "负荷情况"] = "负荷高小区" # 其他场景：>50%
```

**4G 场景化阈值：**

| 小区类型 | 负荷高阈值 |
|---------|-----------|
| RDC/DC/RGS/GS 开头 | > 90% |
| RD- 开头（农村） | > 70% |
| 其他场景 | > 50% |

---

## 4. 扇区映射算法

### 4.1 共站同覆盖映射表加载

```python
def load_cog_coverage_mapping(logger) -> pd.DataFrame:
    # 1. 读取共站同覆盖文件
    df = pd.read_excel(path, dtype_backend="numpy_nullable")

    # 2. 确保必要列存在
    if "CGI" not in df.columns or "共站同覆盖名" not in df.columns:
        return empty_df

    # 3. 去重（同一 CGI 取第一条）
    mapping = df.drop_duplicates(subset=["CGI"], keep="first")

    # 4. 额外映射字段
    extra_cols = ["路测网格", "乡镇街道", "是否覆盖层", "小区所属区域"]
    extra_cols = [col for col in extra_cols if col in df.columns]

    return mapping
```

### 4.2 字段映射应用

```python
def apply_sector_mapping(table, mapping, cgi_column, logger):

    for col in mapping_cols:
        # 创建 CGI -> 目标字段 的映射字典
        col_mapping = dict(zip(mapping["CGI"], mapping[col]))

        # 扇区字段特殊处理：共站同覆盖名 -> 扇区
        target_col = "扇区" if col == "共站同覆盖名" else col

        # 替换更新
        table[target_col] = table[cgi_column].map(col_mapping)
```

---

## 5. 表合并算法

### 5.1 5G 容量表构建

**输入源：** `5g_week` + `5g_day` + `5g_mr` + `5g_kpi`

**合并顺序：**
1. 以 `5g_week` 为基础，按 `NCGI` 去重
2. 左连接 `day_group`（天数据聚合）
3. 左连接 `weekday_group`（工作日聚合）
4. 左连接 `weekend_group`（周末聚合）
5. 左连接 `mr_group`（MR 覆盖数据）
6. 左连接 `kpi_group`（KPI 数据）

### 5.2 4G 容量表构建

**输入源：** `4g_week` + `4g_day` + `4g_mr`

**合并顺序：**
1. 以 `4g_week` 为基础，按 `CGI` 去重
2. 左连接 `day_group`（天数据聚合）
3. 左连接 `week_metrics`（周的 PRB 等指标）
4. 左连接 `weekday_group`（工作日聚合）
5. 左连接 `weekend_group`（周末聚合）
6. 左连接 `mr_group`（MR 覆盖数据）

### 5.3 45G 总表合并

```python
def build_45g_table(table_5g, table_4g):
    # 1. 插入网络制式标识
    merged_5g.insert(0, "网络制式", "5G")
    merged_4g.insert(0, "网络制式", "4G")

    # 2. 统一列名
    merged_5g.rename(columns={"NCGI": "CGI/NCGI", ...})
    merged_4g.rename(columns={"CGI": "CGI/NCGI"})

    # 3. 补齐双方缺失的列
    for col in all_columns:
        if col not in df: df[col] = pd.NA

    # 4. 纵向拼接
    return pd.concat([merged_5g, merged_4g], ignore_index=True)
```

---

## 6. 输出字段说明

### 6.1 输出文件

| 输出文件 | 内容 |
|---------|------|
| `合成_容量表_5G{时间戳}.xlsx` | 5G 小区综合容量表 |
| `合成_容量表_4G{时间戳}.xlsx` | 4G 小区综合容量表 |
| `容量表_45G{时间戳}.xlsx` | 4G/5G 合并总表 |

### 6.2 关键输出字段

| 字段分类 | 字段名 | 数据来源/计算方式 |
|---------|-------|------------------|
| 基础信息 | 记录开始/结束时间, 地市, 网元状态, 小区名称, band, 场景 | 周表 |
| 流量指标 | 日均流量, 流量系数, 流量排名升序 | 天表均值计算 |
| 负荷指标 | 自忙时利用率, 负荷情况 | 天表均值计算 |
| 工作日/周末 | 工作日/周末自忙时利用率, 日均流量, RRC连接最大数 | 按工作日/周末筛选后聚合 |
| PRB指标 | 自忙时上行/下行PRB平均利用率, PDCCH CCE占用率 | 天表均值 |
| RRC连接 | 自忙时RRC连接最大数, 有效RRC连接平均/最大数 | 天表均值 |
| 流量分类 | 流量是否正常, 长尾小区 | 基于流量系数的分类 |
| MR覆盖 | MRO移动总采样点, 覆盖率, 平均TA | MR表聚合 |
| VoNR/VOLTE | VoNR语音话务量 / 语音话务量Erl | KPI表/周表 |
| 扇区信息 | 扇区, 路测网格, 乡镇街道, 是否覆盖层, 小区所属区域 | 共站同覆盖映射表 |

### 6.2 输出表格字段详情

#### 6.2.1 5G 合成容量表 (合成_容量表_5G*.xlsx)

| 字段分类 | 字段名 | 数据来源/说明 |
|---------|-------|--------------|
| 基础信息 | 记录开始时间, 记录结束时间 | 来自周表 |
| 基础信息 | 地市 | 来自周表 |
| 基础信息 | NCGI | 来自周表 |
| 基础信息 | 网元状态 | 来自周表 |
| 基础信息 | 小区名称 | 来自周表 |
| 基础信息 | 扇区 | 来自共站同覆盖映射表 |
| 基础信息 | band | 来自周表"使用频段" |
| 基础信息 | 场景V容量表 | 来自周表"场景1"或"一级场景" |
| 基础信息 | TYPE | 固定空值 |
| 基础信息 | 物理站 | 来自周表"站点名称" |
| 流量分析 | 流量是否正常 | 基于流量系数分类 |
| 流量分析 | 负荷情况 | 基于自忙时利用率判定 |
| 流量分析 | 流量排名升序 | 日均流量升序排名 |
| 流量分析 | 长尾小区 | 基于流量和利用率分类 |
| 流量分析 | 流量系数 | 日均流量/全网日均流量 |
| 流量分析 | 日均流量 | 天表均值 |
| 核心指标 | 自忙时利用率 | 天表均值 |
| 核心指标 | VoNR语音话务量 | KPI表均值 |
| 核心指标 | 自忙时上行流量, 自忙时下行流量, 自忙时总流量 | 天表均值计算 |
| 工作日/周末 | 工作日自忙时利用率, 工作日日均流量, 工作日自忙时RRC连接最大数 | 工作日聚合 |
| 工作日/周末 | 周末自忙时利用率, 周末日均流量, 周末自忙时RRC连接最大数 | 周末聚合 |
| PRB指标 | 自忙时上行PRB平均利用率, 自忙时下行PRB平均利用率 | 天表均值 |
| PRB指标 | 自忙时PDCCH信道CCE占用率 | 天表均值 |
| RRC指标 | 自忙时RRC连接最大数, 自忙时有效RRC连接最大数 | 天表均值 |
| RRC指标 | 自忙时有效RRC连接平均数 | 天表均值 |
| MR覆盖 | MRO移动总采样点 | MR表均值 |
| MR覆盖 | MRO移动覆盖率 | MR表计算：强覆盖/总采样 |
| MR覆盖 | 平均TA米 | MR表均值 |
| 预警标识 | 是否全省高负荷预警小区（集团口径） | 固定空值 |
| 预警标识 | 是否高负荷待扩容小区 | 来自周表 |
| 预警标识 | 是否全省高负荷预警小区（省内口径） | 固定空值 |

#### 6.2.2 4G 合成容量表 (合成_容量表_4G*.xlsx)

| 字段分类 | 字段名 | 数据来源/说明 |
|---------|-------|--------------|
| 基础信息 | 记录开始时间, 记录结束时间 | 来自周表 |
| 基础信息 | 地市 | 来自周表"所属地市" |
| 基础信息 | CGI | 来自周表 |
| 基础信息 | 网元状态 | 来自周表 |
| 基础信息 | 小区名称 | 来自周表 |
| 基础信息 | 扇区 | 来自共站同覆盖映射表 |
| 基础信息 | band | 来自周表"使用频段"或"频点" |
| 基础信息 | 场景V容量表 | 来自周表"场景" |
| 基础信息 | TYPE | 固定空值 |
| 基础信息 | 物理站 | 来自周表"所属站点名称" |
| 流量分析 | 流量是否正常 | 基于流量系数分类 |
| 流量分析 | 负荷情况 | 基于小区类型场景化判定 |
| 流量分析 | 流量排名升序 | 日均流量升序排名 |
| 流量分析 | 长尾小区 | 基于流量和利用率分类 |
| 流量分析 | 流量系数 | 日均流量/全网日均流量 |
| 流量分析 | 日均流量 | 天表均值 |
| 核心指标 | 自忙时利用率 | 天表上下行PRB最大值 |
| 核心指标 | 语音话务量Erl (VOLTE/VoNR) | 来自周表"VOLTE语音话务量" |
| 核心指标 | 自忙时上行流量, 自忙时下行流量, 自忙时总流量 | 天表均值 |
| 工作日/周末 | 工作日自忙时利用率, 工作日日均流量, 工作日自忙时RRC连接最大数 | 工作日聚合 |
| 工作日/周末 | 周末自忙时利用率, 周末日均流量, 周末自忙时RRC连接最大数 | 周末聚合 |
| PRB指标 | 自忙时上行PRB平均利用率, 自忙时下行PRB平均利用率 | 天表均值或周表补充 |
| PRB指标 | 自忙时PDCCH信道CCE占用率 | 天表均值或周表补充 |
| RRC指标 | 自忙时RRC连接最大数, 自忙时有效RRC连接最大数 | 天表均值或周表补充 |
| RRC指标 | 自忙时有效RRC连接平均数 | 天表均值 |
| MR覆盖 | MRO移动总采样点 | MR表均值 |
| MR覆盖 | MRO移动覆盖率 | MR表计算：有效点/总采样 |
| MR覆盖 | 平均TA米 | MR表均值 |
| 预警标识 | 是否全省高负荷预警小区（集团口径） | 来自周表"是否高流量预警小区" |
| 预警标识 | 是否高负荷待扩容小区 | 来自周表 |
| 预警标识 | 是否全省高负荷预警小区（省内口径） | 来自周表"是否高流量预警小区" |

#### 6.2.3 45G 总表 (容量表_45G*.xlsx)

| 字段分类 | 字段名 | 说明 |
|---------|-------|------|
| 基础信息 | 网络制式 | "5G" 或 "4G" |
| 基础信息 | CGI/NCGI | 5G为NCGI, 4G为CGI |
| 其他字段 | （同上） | 与对应单制式表字段相同 |

---

## 7. 流程图

```
┌─────────────────────────────────────────────────────────────┐
│                      程序启动                                │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              1. 加载源数据文件                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ • 扫描 data/ 目录匹配各类型文件                        │  │
│  │ • 选择最新版本文件                                     │  │
│  │ • 解析 Excel → DataFrame                              │  │
│  │ • 加载共站同覆盖映射表                                 │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              2. 构建 5G 容量表                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ NCGI 去重 → 天表聚合 → 工作日/周末聚合                 │  │
│  │ → MR 聚合 → KPI 聚合 → 计算流量系数/分类              │  │
│  │ → 扇区映射 → 字段重排输出                             │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              3. 构建 4G 容量表                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ CGI 去重 → 天表聚合 → 周指标 → 工作日/周末聚合         │  │
│  │ → MR 聚合 → 计算流量系数/分类/负荷判定                 │  │
│  │ → 扇区映射 → 字段重排输出                             │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              4. 合并 45G 总表                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ • 插入网络制式标识                                    │  │
│  │ • 统一列名 (NCGI → CGI/NCGI)                         │  │
│  │ • 补齐双方缺失列                                      │  │
│  │ • 纵向拼接                                            │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              5. 输出 Excel 文件                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ • 合成_容量表_5G{时间戳}.xlsx                         │  │
│  │ • 合成_容量表_4G{时间戳}.xlsx                         │  │
│  │ • 容量表_45G{时间戳}.xlsx                             │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 专项功能：合成45G流量表

### 8.1 功能概述

合成45G流量表是NQI工具的专项功能，用于自动从大数据平台下载8个数据源表，然后通过核心算法合成45G联合容量表。

**功能特点：**
- 自动从大数据平台并行下载8个数据表
- 多进程并行下载提升效率
- 自动识别并合成45G联合总表
- 输出目录按时间戳组织，便于追溯

### 8.2 数据源表

| 序号 | 表名 | 说明 |
|-----|------|------|
| 1 | 5G小区容量-周 | 5G小区周粒度容量数据 |
| 2 | 5G小区容量报表 | 5G小区天粒度容量数据 |
| 3 | 5GMR覆盖-小区天 | 5G MR测量报告覆盖数据 |
| 4 | 5G小区性能KPI报表 | 5G小区性能KPI数据 |
| 5 | 重要场景-周 | 4G重要场景周数据 |
| 6 | 重要场景-天 | 4G重要场景天数据 |
| 7 | 4GMR覆盖-小区天 | 4G MR测量报告覆盖数据 |
| 8 | 共站同覆盖小区_4g_5g | 4G/5G共站同覆盖映射表 |

### 8.3 使用流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 选择表格：数据表 → 专项功能 → 合成45G流量表              │
│ 2. 选择时间：周选择器 → 选择第X周（周一至周日）              │
│ 3. 选择地市：地市选择 → 选择目标地市                         │
│ 4. 点击提取：系统自动执行以下流程                            │
└─────────────────────────────────────────────────────────────┘
```

### 8.4 处理流程

```mermaid
flowchart TD
    A[用户选择"合成45G流量表"] --> B[选择周粒度时间范围]
    B --> C[点击提取按钮]
    C --> D[多进程并行下载8个数据表]
    D --> E[下载完成，汇总数据]
    E --> F[构建5G容量表]
    E --> G[构建4G容量表]
    F --> H[合并45G总表]
    G --> H
    H --> I[生成输出目录]
    I --> J[保存源数据 + 合成表]

    D --> D1[5G小区容量-周]
    D --> D2[5G小区容量报表]
    D --> D3[5G MR覆盖]
    D --> D4[5G KPI报表]
    D --> D5[重要场景-周]
    D --> D6[重要场景-天]
    D --> D7[4G MR覆盖]
    D --> D8[共站同覆盖]
```

### 8.5 输出目录结构

```
data_output/
└── 45G流量表_YYYYMMDD_YYYYMMDD/
    ├── 原始数据/
    │   ├── 5G小区容量-周_YYYYMMDD_YYYYMMDD.xlsx
    │   ├── 5G小区容量报表_YYYYMMDD_YYYYMMDD.xlsx
    │   ├── 5GMR覆盖-小区天_YYYYMMDD_YYYYMMDD.xlsx
    │   ├── 5G小区性能KPI报表_YYYYMMDD_YYYYMMDD.xlsx
    │   ├── 重要场景-周_YYYYMMDD_YYYYMMDD.xlsx
    │   ├── 重要场景-天_YYYYMMDD_YYYYMMDD.xlsx
    │   ├── 4GMR覆盖-小区天_YYYYMMDD_YYYYMMDD.xlsx
    │   └── 共站同覆盖小区_4g_5g_YYYYMMDD_YYYYMMDD.xlsx
    └── 合成表/
        ├── 合成_容量表_5G_YYYYMMDD_YYYYMMDD.xlsx
        ├── 合成_容量表_4G_YYYYMMDD_YYYYMMDD.xlsx
        └── 容量表_45G_YYYYMMDD_YYYYMMDD.xlsx
```

### 8.6 核心算法

#### 8.6.1 5G容量表构建

**输入源：** 5G小区容量-周 + 5G小区容量报表 + 5GMR覆盖 + 5G KPI

**处理步骤：**
1. NCGI去重（取第一条记录）
2. 天数据聚合（按NCGI分组，计算均值）
3. 工作日/周末分离聚合
4. MR数据聚合（采样点、覆盖率、TA）
5. KPI数据聚合（VoNR话务量）
6. 计算流量系数、流量排名、长尾小区分类
7. 负荷情况判定
8. 扇区映射（共站同覆盖）

#### 8.6.2 4G容量表构建

**输入源：** 重要场景-周 + 重要场景-天 + 4G MR覆盖

**处理步骤：**
1. CGI去重（取第一条记录）
2. 天数据聚合（按CGI分组，计算均值）
3. 计算自忙时利用率（上下行PRB最大值）
4. MR数据聚合（采样点、覆盖率）
5. 计算流量系数、流量排名、长尾小区分类
6. 负荷情况判定（场景化阈值）
7. 扇区映射（共站同覆盖）

#### 8.6.3 45G总表合并

1. 插入网络制式标识（"5G" / "4G"）
2. 统一列名（NCGI/CGI → CGI/NCGI）
3. 补齐双方缺失列（填充NA）
4. 纵向拼接

### 8.7 关键配置

**模块路径：** `core/flow_table_builder.py`

**核心类：** `FlowTableBuilder`

```python
from core.flow_table_builder import synthesize_45g_flow_table

# 调用示例
success = synthesize_45g_flow_table(
    session,           # 已登录的requests Session
    city="阳江",      # 地市
    week_start=datetime(2026, 6, 2),  # 周一日期
    progress_callback=callback  # 进度回调
)
```

---

## 9. 性能优化要点

1. **向量化计算**：长尾小区分类、流量是否正常等均使用向量化操作，避免 `apply` 的行迭代开销
2. **分步聚合**：工作日/周末数据通过布尔索引分离后各自聚合，减少数据量
3. **安全除法**：使用 `replace(0, pd.NA)` 避免除零错误
4. **多线程日志**：GUI 中使用独立线程处理数据，主线程更新 UI，通过 Queue 通信
